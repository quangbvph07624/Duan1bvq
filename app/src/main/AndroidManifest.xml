<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duanbvq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.EndActivity"></activity>
        <activity android:name=".activity.QuizActivity" />
        <activity android:name=".activity.ExxampleActivity" />
        <activity android:name=".activity.OntapActivity" />
        <activity android:name=".activity.GhiNhoActivity" />
        <activity android:name=".activity.BienBaoActivity" />
        <activity android:name=".activity.WaitTracnghiemActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.WelcomesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>